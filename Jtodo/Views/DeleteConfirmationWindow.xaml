<Window x:Class="Jtodo.Views.DeleteConfirmationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Confirm Delete" 
        SizeToContent="Height"
        MinHeight="250" 
        MaxHeight="500"
        Width="450"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="White"
        FontFamily="Segoe UI">
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Warning Icon -->
        <Border Grid.Row="0" 
                Background="#FFF3CD" 
                CornerRadius="10"
                Padding="15"
                Margin="0,0,0,15">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="⚠" 
                           FontSize="24" 
                           VerticalAlignment="Center"
                           Margin="0,0,10,0"/>
                <TextBlock Text="Warning: This action can't be undone!"
                           FontSize="14"
                           FontWeight="SemiBold"
                           Foreground="#856404"
                           VerticalAlignment="Center"/>
            </StackPanel>
        </Border>

        <!-- Title to Delete -->
        <StackPanel Grid.Row="1" Margin="0,0,0,15">
            <TextBlock Text="List of Delete:" 
                       FontSize="12" 
                       Foreground="#666"
                       Margin="0,0,0,5"/>
            <TextBlock x:Name="TitleTextBlock"
                       FontSize="16"
                       FontWeight="Bold"
                       Foreground="#333"
                       TextWrapping="Wrap"/>
        </StackPanel>

        <!-- Instruction -->
        <TextBlock Grid.Row="2"
                   Text="Enter text below for confirm:"
                   FontSize="13"
                   Foreground="#666"
                   Margin="0,0,0,5"/>

        <!-- Required Text to Type -->
        <Border Grid.Row="3"
                Background="#F0F0F0"
                CornerRadius="5"
                Padding="10"
                Margin="0,0,0,15">
            <TextBlock x:Name="RequiredTextBlock"
                       FontFamily="Consolas"
                       FontSize="14"
                       Foreground="#E74C3C"
                       FontWeight="Bold"
                       TextWrapping="Wrap"/>
        </Border>

        <!-- Input TextBox -->
        <TextBox x:Name="ConfirmTextBox"
                 Grid.Row="4"
                 FontSize="14"
                 Padding="10"
                 VerticalAlignment="Top"
                 Height="40"
                 TextChanged="ConfirmTextBox_TextChanged"/>

        <!-- Buttons -->
        <StackPanel Grid.Row="5" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right"
                    Margin="0,15,0,0">
            <Button x:Name="CancelButton"
                    Content="Cancel"
                    Width="100"
                    Height="35"
                    Margin="0,0,10,0"
                    Click="CancelButton_Click"
                    Background="White"
                    BorderBrush="#CCC"
                    BorderThickness="1"
                    Cursor="Hand">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border x:Name="Border"
                                            Background="{TemplateBinding Background}"
                                            BorderBrush="{TemplateBinding BorderBrush}"
                                            BorderThickness="{TemplateBinding BorderThickness}"
                                            CornerRadius="5"
                                            Padding="10">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="Border" Property="Background" Value="#F0F0F0"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Button.Style>
            </Button>

            <Button x:Name="DeleteButton"
                    Content="Delete"
                    Width="100"
                    Height="35"
                    Click="DeleteButton_Click"
                    Background="#E74C3C"
                    Foreground="White"
                    BorderThickness="0"
                    IsEnabled="False"
                    Cursor="Hand">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border x:Name="Border"
                                            Background="{TemplateBinding Background}"
                                            CornerRadius="5"
                                            Padding="10">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsEnabled" Value="False">
                                            <Setter TargetName="Border" Property="Background" Value="#CCC"/>
                                            <Setter Property="Foreground" Value="#999"/>
                                        </Trigger>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="Border" Property="Background" Value="#C0392B"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Button.Style>
            </Button>
        </StackPanel>
    </Grid>
</Window>
